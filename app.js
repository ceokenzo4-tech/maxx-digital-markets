(function(){
  const reg=document.querySelector('#registerForm');
  if(reg){reg.addEventListener('submit',function(e){e.preventDefault();const msg=document.querySelector('#message');const name=reg.name.value.trim(),email=reg.email.value.trim(),password=reg.password.value;if(!name||!email||password.length<10){msg.textContent='Complete all fields. Password must be at least 10 characters.';msg.className='notice err';return;}localStorage.setItem('maxx_demo_profile',JSON.stringify({name,email}));localStorage.setItem('maxx_user',JSON.stringify({name,email,demo:true}));localStorage.setItem('maxx_token','demo-'+Date.now());sessionStorage.setItem('maxxDemoSession','active');if(!localStorage.getItem('maxx_demo_account_v2'))localStorage.setItem('maxx_demo_account_v2',JSON.stringify({balance:10000,positions:[],history:[]}));msg.textContent='Demo account created. Opening dashboard…';msg.className='notice';setTimeout(()=>location.href='dashboard.html',300);});}
})();
